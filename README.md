# Configura-o-Ambiente-MultiContainer
Projeto de API REST CRUD desenvolvida em Flask com MySQL, executando em ambiente multi-container usando Docker Compose. Permite criar, listar, atualizar e excluir usuários, com persistência de dados e configuração segura de banco.
